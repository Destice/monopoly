#include "../include/GameBoard.hpp"
#include <gtest/gtest.h>

using namespace ::testing;

TEST(TemporaryTest, tmpTest) { ASSERT_TRUE(true); }

// TEST(FirstTest, tmpTETT) { GameBoard gameBoard{}; }

TEST(SecondTest, checkIfValueFromFieldIsOk) {
  // GameBoard gameBoard{};
  //
  // EXPECT_EQ(gameBoard.getFieldValue(0), 100);
  // EXPECT_EQ(gameBoard.getFieldValue(1), -200);
  // EXPECT_EQ(gameBoard.getFieldValue(2), 50);
}
